apiVersion: v1
kind: ConfigMap
metadata:
  name: blocky-config
  namespace: networking
data:
  config.yml: |-
    ---
    ports:
      dns: 0.0.0.0:53
      http: 0.0.0.0:4000

    log:
      level: info

    bootstrapDns:
      - tcp-tls:1.1.1.1:853
      - tcp-tls:1.0.0.1:853
      - tcp-tls:9.9.9.9:853
      - tcp-tls:8.8.8.8:853

    # https://developers.cloudflare.com/1.1.1.1/encryption/dns-over-tls/#how-it-works
    upstreams:
      strategy: parallel_best
      groups:
        default:
          - tcp-tls:one.one.one.one:853
          - tcp-tls:dns.quad9.net:853

    customDNS:
      # A pin here is break-glass bootstrap for critical infrastructure only --
      # the set of names you need to repair a broken cluster. It is not a
      # general LAN fallback. Ordinary applications are reached through the
      # tailnet, and the headscale ACL is their access control -- they do not
      # get a pin here (#3629, #3617).
      #
      # The one remaining pin names TRAEFIK_IP, the tier-1 public listener
      # address. No pin names TRAEFIK_LAN_IP: tier 2 holds no route (#3718), so
      # that address answers 404 for every hostname and a pin to it resolves to
      # a dead end. traefik, longhorn and hubble each bind the websecurets
      # listener alone, and kubectl port-forward is their break-glass path. A
      # pin for any of the three belongs here only if its route is bound back
      # to websecurelan.
      #
      # unifi lost its pin in wave E (#3722): it is tier 3 now, and its
      # break-glass path is a direct LAN browse to the physical controller at
      # UNIFI_IP, not a pin here. auth is the last pin left; #3724 removes it.
      mapping: # some useful defaults
        auth.${SECRET_PUBLIC_DOMAIN}: ${TRAEFIK_IP}

    blocking:
      loading:
        # "fast": serve DNS immediately on startup; blocklists load in the
        # background and blocking enables once ready. See issue #3342.
        strategy: fast
        # Tolerate a slow/dead source instead of blocking/crashing startup.
        downloads:
          timeout: 30s
          attempts: 3
          cooldown: 10s
        # Don't fail a whole source over a handful of bad lines.
        maxErrorsPerSource: 5
      denylists:
        suspicious:
          - https://raw.githubusercontent.com/PolishFiltersTeam/KADhosts/master/KADhosts.txt
          - https://raw.githubusercontent.com/FadeMind/hosts.extras/master/add.Spam/hosts
          - https://v.firebog.net/hosts/static/w3kbl.txt
        ads:
          - https://adaway.org/hosts.txt
          - https://v.firebog.net/hosts/AdguardDNS.txt
          - https://v.firebog.net/hosts/Admiral.txt
          - https://raw.githubusercontent.com/anudeepND/blacklist/master/adservers.txt
          - https://s3.amazonaws.com/lists.disconnect.me/simple_ad.txt
          - https://v.firebog.net/hosts/Easylist.txt
          - https://pgl.yoyo.org/adservers/serverlist.php?hostformat=hosts&showintro=0&mimetype=plaintext
          - https://raw.githubusercontent.com/FadeMind/hosts.extras/master/UncheckyAds/hosts
          - https://raw.githubusercontent.com/bigdargon/hostsVN/master/hosts
        trackers:
          - https://v.firebog.net/hosts/Easyprivacy.txt
          - https://v.firebog.net/hosts/Prigent-Ads.txt
          - https://raw.githubusercontent.com/FadeMind/hosts.extras/master/add.2o7Net/hosts
          - https://raw.githubusercontent.com/crazy-max/WindowsSpyBlocker/master/data/hosts/spy.txt
          - https://hostfiles.frogeye.fr/firstparty-trackers-hosts.txt
        misc:
          - https://raw.githubusercontent.com/DandelionSprout/adfilt/master/Alternate%20versions%20Anti-Malware%20List/AntiMalwareHosts.txt
          - https://osint.digitalside.it/Threat-Intel/lists/latestdomains.txt
          - https://s3.amazonaws.com/lists.disconnect.me/simple_malvertising.txt
          - https://v.firebog.net/hosts/Prigent-Crypto.txt
          - https://raw.githubusercontent.com/FadeMind/hosts.extras/master/add.Risk/hosts
          - https://phishing.army/download/phishing_army_blocklist_extended.txt
          - https://gitlab.com/quidsup/notrack-blocklists/raw/master/notrack-malware.txt
          - https://v.firebog.net/hosts/RPiList-Malware.txt
          - https://v.firebog.net/hosts/RPiList-Phishing.txt
          - https://raw.githubusercontent.com/Spam404/lists/master/main-blacklist.txt
          - https://raw.githubusercontent.com/AssoEchap/stalkerware-indicators/master/generated/hosts
      allowlists:
        suspicious:
          - https://raw.githubusercontent.com/anudeepND/whitelist/master/domains/whitelist.txt
        ads:
          - https://raw.githubusercontent.com/anudeepND/whitelist/master/domains/whitelist.txt
        trackers:
          - https://raw.githubusercontent.com/anudeepND/whitelist/master/domains/whitelist.txt
        misc:
          - https://raw.githubusercontent.com/anudeepND/whitelist/master/domains/whitelist.txt
      clientGroupsBlock:
        default:
          - suspicious
          - ads
          - trackers
          - misc

    caching:
      minTime: 5m
      prefetching: true

    redis:
      required: true
      address: blocky-valkey:6379
      username: default
      password: ${BLOCKY_VALKEY_PASSWORD}

    prometheus:
      enable: true
      path: /metrics

